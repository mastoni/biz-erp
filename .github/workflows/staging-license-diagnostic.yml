name: Staging License Runtime Diagnostic

on:
  workflow_dispatch:

jobs:
  diagnostic:
    runs-on: ubuntu-latest
    environment: staging
    steps:
      - name: Apply Minimal /beli/ Routing Fix and Validate
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.STAGING_HOST }}
          username: ${{ secrets.STAGING_USERNAME }}
          key: ${{ secrets.STAGING_SSH_KEY }}
          port: 22
          script: |
            set -euo pipefail

            CONF_FILE="/opt/skmnet-erp-staging/repo/apps/api/nginx/nginx.staging.conf"
            TIMESTAMP=$(date +%Y%m%d_%H%M%S)
            BACKUP_FILE="${CONF_FILE}.bak_${TIMESTAMP}"

            echo "=== STEP 1: BACKUP CURRENT CONFIG ==="
            cp -a "$CONF_FILE" "$BACKUP_FILE"
            echo "Backup created at: $BACKUP_FILE"

            echo "=== STEP 2: APPLY MINIMAL /BELI/ ROUTING EDIT ==="
            if grep -q 'location /beli/' "$CONF_FILE"; then
              echo "location /beli/ already present in $CONF_FILE"
            else
              node -e '
              const fs = require("fs");
              const confPath = process.argv[1];
              let content = fs.readFileSync(confPath, "utf8");
              
              const beliBlock = `        # 3.1 Public Order Page (/beli/*)
        location /beli/ {
            proxy_pass http://bukuwarung_license_server:3000;
            proxy_http_version 1.1;
            proxy_set_header Host \$host;
            proxy_set_header X-Real-IP \$remote_addr;
            proxy_set_header X-Forwarded-For \$proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto \$scheme;
            proxy_connect_timeout 10s;
            proxy_read_timeout 30s;
            proxy_send_timeout 30s;
            proxy_buffering off;
        }\n\n`;

              const targetPattern = /(server_name\s+license\.skmnetwork\.com;[\s\S]*?location\s+\/download\/[\s\S]*?\}\n+)(\s+)(#\s*4\.\s*Admin Dashboard|\s*location\s+\/\s*\{)/;
              
              if (targetPattern.test(content)) {
                content = content.replace(targetPattern, (match, p1, p2, p3) => {
                  return p1 + p2 + beliBlock + p2 + p3;
                });
                fs.writeFileSync(confPath, content, "utf8");
                console.log("Successfully inserted location /beli/ into " + confPath);
              } else {
                const fallbackPattern = /(server_name\s+license\.skmnetwork\.com;[\s\S]*?)(\n\s+location\s+\/\s*\{)/;
                if (fallbackPattern.test(content)) {
                  content = content.replace(fallbackPattern, (match, p1, p2) => {
                    return p1 + "\n" + beliBlock + p2;
                  });
                  fs.writeFileSync(confPath, content, "utf8");
                  console.log("Fallback insertion of location /beli/ successful.");
                } else {
                  console.error("Could not find insertion anchor for license.skmnetwork.com");
                  process.exit(1);
                }
              }
              ' "$CONF_FILE"
            fi

            echo "=== STEP 3: VALIDATE NGINX CONFIGURATION ==="
            if ! docker exec bizerp_staging_nginx nginx -t; then
              echo "ERROR: nginx -t failed! Rolling back immediately..."
              cp -f "$BACKUP_FILE" "$CONF_FILE"
              docker exec bizerp_staging_nginx nginx -t || true
              exit 1
            fi
            echo "nginx -t validation SUCCESSFUL."

            echo "=== STEP 4: VERIFY CONFIG CONTENT (nginx -T) ==="
            docker exec bizerp_staging_nginx nginx -T 2>&1 | grep -A 35 -B 5 -Ei 'server_name.*license\.skmnetwork\.com'

            echo "=== STEP 5: ACTIVATE CONFIG (nginx -s reload) ==="
            docker exec bizerp_staging_nginx nginx -s reload
            echo "Nginx reload SUCCESSFUL."

            echo "=== STEP 6: LIVE VALIDATION ==="
            echo "--- 1. Health endpoint ---"
            curl -sSI -m 10 https://license.skmnetwork.com/health

            echo "--- 2. Public Order Page Header ---"
            curl -sSI -m 10 https://license.skmnetwork.com/beli/buku-warung

            echo "--- 3. Public Order Page Content Preview ---"
            curl -sS -m 10 https://license.skmnetwork.com/beli/buku-warung | head -n 30

            echo "--- 4. Admin Root Preview ---"
            curl -sS -m 10 https://license.skmnetwork.com/ | head -n 20

            echo "--- 5. Download endpoint ---"
            curl -sSI -m 10 https://license.skmnetwork.com/download/buku-warung

            echo "--- 6. Download guide endpoint ---"
            curl -sSI -m 10 https://license.skmnetwork.com/download/buku-warung/panduan

            echo "=== STEP 7: REGRESSION CHECK ==="
            docker ps --format 'table {{.Names}}	{{.Status}}'

            echo "=== GATE 3C REMEDIATION COMPLETED ==="
